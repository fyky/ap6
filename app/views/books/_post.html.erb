      <h1>New book</h1>
      <%= form_with model:Book.new, local:true do |f| %>
        <p>Title</p>
        <%= f.text_field :title %>
        <p>Opinion</p>
        <%= f.text_area :body %>
        <p></p>
        <%= f.submit 'Create Book' %>
      <% end %>