<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'users/info', user: @user %>
      <%= render 'books/post', book: @book %>
    </div>

    <div class="col-md-8 offset-md-1">
      <h1>Book detail</h1>
      <div class="book_detail">
        <%= render "books/commentcount" , book: @book %>       
      </div>
      
      <div class="book_comment">
　　　<%= render "book_comments/comment", book_comments: @book.book_comments, book: @book %>
        <%= form_with(model:[@book, @comment]) do |f| %>
          <%= f.text_area :comment, size: "80x5", id: "book_comment_form" %><br>
          <%= f.submit "送信" %></p>
        <% end %>
　　　</div>

    </div>
</div>